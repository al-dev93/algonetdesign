import React from 'react';

/**
 *
 * @description login to the administration page
 * @export
 * @return {React.JSX.Element}
 */
export function Auth(): React.JSX.Element {
  return <div>Page de connexion</div>;
}
